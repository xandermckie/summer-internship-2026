# Prompting Notes

## Exercise 2 - Prompt Comparison

Prompt 1 returned an extremely simple and short response that only gave generic information and wouldn't be helpful for any real implementation. For example, it showed a basic requests.get() call with a placeholder URL and no guidance on how to structure parameters or handle API-specific response formats. However, prompt 2 with the specific context and demands given to Claude allowed it to return a very detailed step-by-step on using the Open-Meteo API. Specifically, it identified the exact endpoint (https://api.open-meteo.com/v1/forecast), provided Kansas City's coordinates, and explained how to extract tomorrow's temperature from index [1] of the response arrays. Additionally, prompt 1 only mentioned response.json() in passing, while prompt 2 showed the complete nested JSON structure and how to navigate it with data['daily']['temperature_2m_max'][1]. The difference demonstrates that vague prompts yield generic boilerplate, while concrete requirements (API name, location, specific data needed) enable Claude to provide actionable, implementation-ready code.
